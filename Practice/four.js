const myObject ={
    js:"javascript",
    cpp:"C++",
    rb:"rubi",
    swift:"swift by apple"
}

for (const key in myObject) {
   console.log(`${key} shortcut name for ${myObject[key]}`);
        
    }
